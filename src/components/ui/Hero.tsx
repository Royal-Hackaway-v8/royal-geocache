import React from "react";
import Map from "./Map";

const Hero = () => {
	return (
		<div className=" h-screen flex flex-col justify-center items-center w-screen">
			<Map />
		</div>
	);
};

export default Hero;
